{
  "dependencies": {
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "@open-wc/testing": "^3.1.7",
    "@web/dev-server": "^0.1.36",
    "@web/test-runner": "^0.15.1",
    "@web/test-runner-playwright": "^0.9.0"

  },
  "scripts": {
    "test": "web-test-runner test/**/*.test.js --node-resolve --playwright --browsers firefox --watch"
  }
}
